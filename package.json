{
  "name": "theo-blastoff",
  "private": true,
  "type": "module",
  "description": "Blast Off, Theo! — a counting game. Static site; deps are test-tooling only.",
  "scripts": {
    "verify": "./tools/verify.sh"
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  }
}
